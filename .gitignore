# Godot 4+ specific ignores
.godot/
/android/
# Godot default generated project settings
.godot/
# Godot imported files directory
.import/
# User-specific settings (usually safe to ignore)
*.user
# Any saved editor layouts
/editor/
# Default build output directory for export templates
/bin/
# All resource files that are not explicitly loaded by the engine
# (helps keep the repository cleaner)
*.import
